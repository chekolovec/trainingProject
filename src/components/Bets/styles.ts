import { StyleSheet } from "react-native";

export default StyleSheet.create({
  crossWrapper: {
    display: "flex",
    flexDirection: "row-reverse",
  },
  betsContainer: {
    padding: 20,
  },
});
